# Python and Environment
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
Pipfile.lock  # If using Pipenv; remove if not

# Jupyter/IPython
.ipynb_checkpoints/
*.ipynb_checkpoints

# ML Models and Checkpoints (expanded for common formats)
models/checkpoints/
models/final/*.keras
models/final/*.h5
models/final/*.joblib
models/final/*.npz
models/final/*.json
models/final/*.pb  # TensorFlow saved models
models/final/*.tflite  # If optimizing for mobile

# Data (keep samples if small and essential)
data/processed/
data/splits/
data/raw/
data/*.csv  # If large; adjust if sharing small datasets

# Configs and Secrets
config/config.yaml  # If sensitive; else, include a template
.env
secrets/

# Logs and Temp
logs/
*.log
tmp/
temp/

# OS-Specific
.DS_Store
Thumbs.db

# Other Project-Specific (from your code: avoid ignoring code!)
# src/  # REMOVE this—don't ignore your source code!
# co  # REMOVE or complete (e.g., config/ if meant to ignore whole dir)

# Large Media/Outputs (for your CV project)
videos/
images/
outputs/
test/
tests/asl_alphabet_test
documentation/
*.mp3  # gTTS audio outputs
*.mp4  # Recorded gestures
